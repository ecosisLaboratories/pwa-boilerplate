import { CapacitorConfig } from '@capacitor/cli'

// TODO dotenv

const config: CapacitorConfig = {
  appId: 'com.elabsbox.app',
  appName: 'Box',
  webDir: 'dist',
  bundledWebRuntime: false
}

export default config
